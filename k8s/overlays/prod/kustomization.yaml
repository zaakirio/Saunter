apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: walkguide
resources:
  - ../../base

# Image tag bumped by CI on every main merge (`kustomize edit set image`)
images:
  - name: walkguide
    newName: ghcr.io/REPLACE_OWNER/walkguide
    newTag: latest
